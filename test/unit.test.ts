import { strict as assert } from 'node:assert';
import { describe } from 'mocha';

// const usernameLandscapeArtist = 'landscape-artist';
// const setName = 'tropical-island';
// const userData = // Read from file.
// const userCollection = // Get from userData.
// const userCollectionMap = // Get from convertCollectionToMap()

describe('builder catalogue challenge', () => {
	describe('setsWhichCanBeBuiltWithCollection()', async () => {
		it('can get sets which user can build with collection', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully invalid collections', async () => {
			assert.equal(true, true);
		});
	});

	describe('getCollaboratorsForCollectionAndSet()', async () => {
		it('can get collaborators which a user can build specified set with', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully non-existant user', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully non-existant set', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully invalid set', async () => {
			assert.equal(true, true);
		});
	});

	describe('userCanBuildSet()', async () => {
		it('can determine if user can build specified set', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully invalid set', async () => {
			assert.equal(true, true);
		});
	});

	describe('combineCollections()', async () => {
		it('can combine collections', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully invalid collection', async () => {
			assert.equal(true, true);
		});
	});

	describe('convertCollectionToMap()', async () => {
		it('can convert user collection to map', async () => {
			assert.equal(true, true);
		});

		it('handles gracefully invalid collection', async () => {
			assert.equal(true, true);
		});
	});
});
